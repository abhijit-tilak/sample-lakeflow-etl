-- Databricks notebook source
-- The 'explorations' folder is used for ad-hoc notebooks
-- to explore the data produced by this pipeline.

USE CATALOG `users`;
USE SCHEMA `abhijit_tilak`;

SELECT * from sample_taxis_atilak_test_lakeflow;
